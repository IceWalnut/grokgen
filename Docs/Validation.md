# 验证标准

**执行文档里每个 round 的「判据」一栏，引用的就是本文件定义的层级与 VS 编号。**

本文件**两端共用**：`server/`（FastAPI 网关）与 `app/`（Android 客户端）
遵守同一套层级和同一套 VS 编号。理由和接口契约放根目录是同一个 ——
**跨两端的验证场景不该被编两个号**，验证的规矩也不该被抄两遍。
两端不同的只有**命令**，见 §2 的表。

---

## 1. 这个项目为什么需要专门的验证标准

四件事在这个项目里机器判不了，而它们恰好是产品的核心：

1. **视频画面是不是正常的。** 任务成功、格式正确、时长对，画面仍然可能是一片黑或纯噪声。
2. **首帧图有没有真的生效。** 首帧参数传错时，任务会**正常成功**，只是悄悄变成了一个 T2VA。
3. **手机上播得顺不顺。** 能不能拖进度条、起播要等多久。
4. **界面好不好看**（M2 起）。对比度可以算，**节奏、留白、层次只能看**。

⇒ **Level 0–3 的自动化不是为了替代眼睛，而是为了保证在两次人眼检查之间不发生静默回退。**

---

## 2. 验证层级

### Level 0：文档检查

- `Docs/TODO.md`、`Docs/ContextPack.md` 已按本轮改动更新
- 受影响的需求 / 契约 / 架构 / 执行文档已同步
- 本轮若推翻了某个决策，**改的是上游文档，不是绕过它**（`AGENTS.md` §2）

### Level 1：构建与分层检查

| 端 | 命令 |
|---|---|
| server | `cd server && .venv/bin/python -m pytest --collect-only`（导入期错误在这里暴露） |
| app | `./gradlew build` |

⚠️ **网关的 pytest 必须在 `server/` 目录下跑，不能在仓库根目录跑。**
`asyncio_mode = "auto"` 写在 `server/pyproject.toml` 里，从根目录跑 pytest 读不到它，
于是所有裸 `async def` 测试都报 `async def functions are not natively supported`。
实测：根目录跑是 10 failed / 80 passed，`server/` 目录跑是 90 passed ——
**那 10 条是假失败**，容易被当成真的回归去查。

⭐ **网关有一条可执行的分层判据**（`VS-13`，落在 `server/tests/test_layering.py`）：

> 扫 `app/` 下每个 `.py` 的 AST，断言除 `comfy/http_client.py` 外
> **没有模块 import HTTP 库，也没有 ComfyUI 地址的字符串字面量**。

这条是架构文档 §3 那个"唯一硬约束"的检验形式。

⚠️ 它最初写成「不连服务器时全部测试通过」，**那是个消极判据** ——
有人在 `core/` 里加了 HTTP 调用而测试恰好没走到那条路，它照样是绿的。
改成扫源码之后，违例时报出的是**具体哪个文件的哪一行**。

⚠️ 硬编码那条只看**代码里的字符串字面量**，跳过 docstring ——
文档里解释默认地址是无害的，拿 docstring 发不了请求。

### Level 2：自动化测试（不需要 GPU）

| 端 | 命令 |
|---|---|
| server | `cd server && .venv/bin/python -m pytest -m "not integration"` |
| app | `./gradlew test` |

这一层跑在**开发机**上，不占 GPU、不连服务器。
**workflow 构造、帧数换算、prompt 拼装、状态机全部属于这一层** ——
它们是纯逻辑，没有理由等到占用 GPU 才发现写错了。

⚠️ **workflow 的 golden 文件不提供自动覆盖开关。**
有了它，"改坏了"和"改好了"就是同一个动作。
期望文件的每一处变化都要人工看过 diff 再手动更新。

落地在 `server/tests/golden/h3_{t2va,i2va,fl2va}.json`。
golden 抓的是单元测试**想不到**的地方：某个值改了、某条连线接错槽位、多了或少了一个节点。
M1R2 实测三条植入缺陷时，每一条都是单元测试与 golden **同时**报错。

### Level 2.5：提交前预检（连 ComfyUI，**不占 GPU**）

```bash
cd server && .venv/bin/python -m pytest -m integration    # 默认跳过，手动触发
```

⭐ **这一层存在的理由**：`POST /prompt` **没有「只校验不执行」的模式** ——
校验一过任务就排进队列，而 MiniMax H3 加载模型是分钟级的固定开销。
所以「提交一次看报错」这个动作本身不便宜，一次拼写错误要付几分钟。

`app/comfy/workflows/validation.py` 拉一次 `/object_info`，检查节点类型存不存在、
必填输入给全没、有没有给未声明的输入、连线指向的节点在不在、COMBO 值在不在候选里。

⚠️ **它只查形状，查不了语义。** 两个 VAE 接反、prompt 写错、连到错误的输出槽位，
形状上都是合法的 —— 那些只有 Level 4 的眼睛能判。

### Level 3：真实链路验证（连 ComfyUI，占 GPU）

```bash
scripts/smoke_m1.sh                  # 端到端冒烟（默认两次生成，约 3 分钟 GPU）
scripts/smoke_m1.sh --cold-start     # 额外量一次冷启动读数，多花几分钟
```

⚠️ **`pytest -m integration` 不在这一层**，它属于上面的 Level 2.5 ——
现有的 integration 测试**全部不占 GPU**。两处都列会让人以为跑它要占显卡，
于是该跑的时候不敢跑。

冒烟脚本的退出码就是出问题的层次，看码即可定位：
`10` 本机环境 · `11` 网络或 ssh · `12` ComfyUI · `13` 网关 ·
`14` 生成 · `15` 产物 · `16` 补验证。

⚠️ **冒烟脚本判不了 Level 4。** 它会在结尾明确列出「需要人眼看的」那几条
并给出材料路径 —— **脚本自己声明它判不了什么**，比让人事后回忆可靠。

⚠️ 跑之前先确认 ComfyUI 没在忙：

```bash
curl -s --noproxy '*' http://icewalnut-1060.tail22a711.ts.net:8188/queue
```

⚠️ 从开发机发请求**必须带 `--noproxy '*'`**，否则本机代理会拦截并返回 502
（runbook §6.4）。

### Level 4：人眼判定（必须做，但不是构建闸门）

- 视频画面正常，不是黑屏或噪声
- I2VA 的首帧**可辨认是上传的那张图**
- 手机上能拖进度条，起播没有明显等待
- **界面好不好看**（M2 起）—— 需求 §7.1 定了方向与色值，
  但**"达到设计要求"和"看着舒服"是两回事**：对比度可以算，节奏和留白只能看。
  ⇒ 每轮附一张真机截图，**做出来的页面在那一轮里就要打磨到能看**，
  不留"以后统一美化"的尾巴（需求 §7.1.5）

⚠️ **这一层只报告"这次看过了"，不参与自动化。**
但**不做不等于通过** —— round 总结里必须写清这一层看了什么、没看什么。

---

## 3. 验证场景（Validation Scenario, VS）

VS 编号**跨里程碑稳定**：一旦分配就不改用途。废弃时保留编号并标注原因。
新增时追加编号，**不复用旧号**。

| 编号 | 场景 | 层级 | 引入于 | 状态 |
|---|---|---|---|---|
| VS-1 | 帧数换算落在 17k+5 格子上 | 2 | M1R2 | ✅ **M1R2**。点位 `1s→39`/`5s→124`/`5.2s→141`/`15s→362`，**外加不变量 `n % 17 == 5`** —— 点位挡不住公式被整体改坏。⚠️ 表里原写的 `1s→22` 是错的，实际是 39（`round(1×24)=24` 已越过 22 那一格） |
| VS-2 | 三种模式的 workflow 形状：T2VA 无 frame、I2VA 只有 `first_frame`、FL2VA 两个都有 | 2 | M1R2 | ✅ **M1R2**。另钉了 turbo 开关是「增删 LoRA 节点」而不是「把强度设 0」 |
| VS-3 | 对齐指令行正确，**且句中秒数是换算后的实际时长** | 2 | M1R2 | ✅ **M1R2**。正向断言那一行确实出现且含 `5.17-second`，并断言 `5.00-second` 不出现。⚠️ 写法有两版（带不带括号），本项目按 V23 模板示例；**两种都不报错，只影响质量，没有便宜的验证方式** |
| VS-4 | 分辨率归一到 32 的倍数，并在响应里报告调整后的值 | 2 | M1R2 | ✅ **M1R2**。`768x432 → 768x448` 且 `notices` 有说明；已对齐时**不产生** notice（避免噪音提示） |
| VS-5 | ComfyUI 接受这张 workflow（`/prompt` 返回 `prompt_id` 且 `node_errors` 为空） | 3 | M1R3 | ✅ **M1R3**。T2VA 一次通过 |
| VS-6 | 产物是 `h264` + `aac` + `mp4`（`ffprobe` 断言） | 3 | M1R3 | ✅ **M1R3**。另外 `736x416` / **124 帧** / **5.167 秒** 与 M1R2 的换算逐项对上 —— 这是那套逻辑的端到端确认 |
| VS-7 | **画面正常**，不是黑屏或噪声 | 4 | M1R3 | ✅ **M1R3**。抽 0.1/2.5/5.0 秒三帧看过，内容符合 prompt，首尾构图不同说明有真实镜头运动。⚠️ 只判了「不是黑屏/噪声/静帧」，**画质好不好没有判据** |
| VS-8 | **I2VA 首帧可辨认是上传的那张图** | 4 | M1R4 | ✅ **M1R4**。两步各验一次：`example.png`（涂鸦，画风独特无含糊）与真上传的跑车图，首帧都可辨认**且不变形** |
| VS-15 | 上传：`asset_id` 带子目录前缀、**用 ComfyUI 返回的 name**、尺寸从落盘路径读 | 2/3 | M1R4 | ✅ **M1R4**。重名改名那条专门写了测试 —— 用错名字会引用到另一张图**且不报错** |
| VS-16 | 画布按首帧图比例推导，不变形 | 2/4 | M1R4 | ✅ **M1R4**。768x768 → **544x544**、736x416 → **736x416**。⚠️ **只验了正面** —— 「强行指定尺寸会压扁」那个对照没实跑 |
| VS-9 | 串行队列：连续提交 3 个任务，任何时刻只有一个处于 `submitted`/`running` | 2 | M1R5 | ✅ **M1R5**。判据是**回放整条转移日志**算峰值，不是采样 —— 状态是分段常数的，逐个转移检查就是完备的。另配两道独立证据：替身在提交边界上自守（不依赖网关记账），以及**正向断言**三个 `submitted` 转移确实发生且顺序正确（否则「三个全卡在 queued」也能让峰值=1 通过）。⚠️ 植入「并发而非串行」验证过会红 |
| VS-10 | 失败时 `failure_reason.detail` **带着 ComfyUI `node_errors` 的原文** | 2 | M1R5 | ✅ **M1R5**。断言**逐字段相等**而非「不为空」—— 压成字符串也能让「不为空」通过。⚠️ 植入 `str(node_errors)` 验证过会红 |
| VS-17 | 取消的三种情形：排队中取消**不产生任何 ComfyUI 调用**；运行中取消会先确认「正在跑的是不是这一个」再中断；终态任务取消返回 409 | 2 | M1R5 | ✅ **M1R5**。排队中那条是**正向断言零次 ComfyUI 调用**。另测了取消与完成的竞态 —— 取消晚一步时产物必须保住。⚠️ `interrupt` 的**真实**落地形式没验证，见 M1R5 总结 §5.2 |
| VS-18 | 提交响应里回报的 `normalized.seed` 与 workflow 里 `RandomNoise` 实际用的 seed **是同一个** | 2 | M1R5 | ✅ **M1R5**。修法是结构性的（整条链路只归一化一次），不是加断言。**另配一条证明风险真实存在的测试**：对同一请求建两次图，seed 确实不同 —— 否则这条判据看起来只是个显然成立的恒等式 |
| VS-11 | **`GET /v1/jobs/{id}/video` 的 Range**：`curl -r 0-1023` 返回 206 且 `Content-Range` 正确 | 2/3 | M1R6 | ✅ **M1R6/R7**。⚠️ 接口名原写作 `/stream`，与契约和实现都对不上，已更正。Range 本身是 Starlette 的 `FileResponse` 实现的，所以测的是**我们的接线** —— 除了 206 与 `Content-Range`，还断言**返回的字节与全量文件逐字节相同**（只查头是负向的：返回正确的头、错误的字节照样通过，而那在手机上表现为拖动后播放错位）。冒烟实测 `bytes 0-1023/702734` ✓ |
| VS-12 | 部署链路：`deploy_server.sh` 能把改动送上去并起服务，端口确实在监听 | 1/3 | M1R1 | ✅ **M1R1**。`version` 回的是刚推的 commit、`vram_total` 是真实读数 —— 后者证明它确实跑在服务器上 |
| VS-14 | **提交前预检**：图对着真实 `/object_info` 查形状，零违例 | 2.5 | M1R3 | ✅ **M1R3**。植入「模型文件名写错」验证过会红。⚠️ **只查形状不查语义** —— VAE 接反、连错槽位形状上都合法 |
| VS-19 | **`running` 状态在真实链路上可达** —— 证明 `queue()` 取 prompt_id 的槽位是对的 | 3 | M1R7 | ✅ **M1R7**。冒烟转移轨迹 `preparing@0s → running@1s → done@75s`。⚠️ 判据记在**转移日志**上而非采样上；没观察到 running 时脚本用「ComfyUI 的 queue_running 是否非空」这条独立证据区分「槽位解析错」与「任务根本没被拾起」——两者的修法在不同的层 |
| VS-20 | **中断一个真实任务后，ComfyUI `/history` 那条记录的真实形状** | 3 | M1R7 | ✅ **M1R7**，**而且推翻了代码里的一条假设**。实测：`status_str="error"`、**`completed=False`**、messages 里有 `execution_interrupted`。网关原来用 `record.completed` 判断「执行结束了没有」，那个字段的真实含义是「成功完成」——于是被中断和报错的任务**永远等不到终态**，而超时默认是关的，即无限轮询。已改用 `JobRecord.finished`（白名单 `{success, error}`）|
| VS-21 | **模型不在显存时一次生成的耗时** | 3 | M1R7 | ✅ **M1R7**，**并更正了一个长期说法**。实测 **94 秒**（热启动 75 秒，只多付约 19 秒），而文档多处写的是「模型加载是分钟级」。⭐ 前提断言有效：`/free` 后 `vram_free` 从 0.91 GB 回到 15.65 GB，**证明确实清空了**，否则量到的仍是热启动。⚠️ **这不是真正的冷启动** —— ComfyUI 进程没重启，模型还在系统内存里。「进程重启后从磁盘读 29 GB」那一次**已由 M2R1 补测：92 秒**，比这里的 94 秒还略快，**「分钟级」那个说法被彻底推翻**。测量条件：4080 SUPER，736x416 / 124 帧 / 8 步 Turbo，1 次 |
| VS-22 | **网关生成的 seed 经双精度 JSON 解析器往返后不变** | 2 | M1R7 | ✅ **M1R7**。起因是实测发现 `jq` 把 seed `3631950862913668918` 读成 `3631950862913669000`，**差 82** —— JSON 数字无类型，很多解析器（含 JavaScript 的 `JSON.parse`）一律当双精度，只能精确到 2**53。seed 的全部用途就是复现，差 82 就什么都复现不了**且不报错**。已把生成范围压到 2**53 以内。⚠️ 判据是**真做一次浮点往返**，不是「小于某个常数」—— 常数写错时那种断言会跟着错 |
| VS-13 | **分层约束**：除 `http_client.py` 外不许 import HTTP 库、不许有地址字面量 | 1 | M1R1 | ✅ **M1R1**。改成扫 AST，比「测试能跑通」这个消极判据强。⚠️ 两条都植入缺陷验证过会红，报的是具体文件与行号 |


#### App 侧（`app/`），VS-30 起

⚠️ **下面这一段全部是「计划中」，一条都还没做过。**
它们由 `app/Docs/implementation/M2_android_prototype.md` §3 引入，
写在这里是因为本文件是 VS 编号的**唯一权威登记处** ——
编号散在两处就会重号。**做完一条改一条的状态，不要整段一次性刷绿。**

| 编号 | 场景 | 层级 | 引入于 | 状态 |
|---|---|---|---|---|
| VS-30 | **App 侧分层约束**：扫 `app/src/main/java` 下每个 `.kt`，HTTP 库的 import 只出现在 `net/` 下，服务器地址与端口的字符串字面量只出现在 `net/ServerAddress.kt` 里 | 1 | M2R1 | ✅ **M2R1**。植入 `import okhttp3.OkHttpClient` 验证过会红，报出 `ConnectScreen.kt:3`；植入含 `tail22a711` 的字面量报出 `ConnectScreen.kt:67`。判据是**扫源码**，不是「不连服务器时测试全通过」——后者是消极判据。⚠️ 写完要故意在 `ui/` 里植入一行 import 确认它会红。⚠️ 这条对 App 比对网关更重要：网关写死地址改一行重新部署即可，**已经装在手机上的 App 连的永远是旧地址**，而 Tailscale IP 在设备重装后会变 |
| VS-31 | **连不上时的四种原因被区分开**：手机没连 Tailscale / 服务器离线 / 网关进程没起来 / **网关活着但 ComfyUI 挂了**，提示各不相同且指向正确的处置动作 | 3 | M2R1 | ⚠️ **M2R1 部分达成，如实记为未完全通过。** 四种情况都在真机上真的制造出来了（手机没装 Tailscale、杀网关进程、`wsl --shutdown`、停 ComfyUI），但**「服务器离线」与「网关进程没起来」分不开** —— 这台服务器的 WSL 用镜像网络，**关闭端口上的连接是被丢弃而不是被拒绝**，两种都只表现为连接超时。已按 §4.1 改上游（需求 F1、架构 §5.0），合并成一档「服务器没有应答」并把三种可能都列进提示。⚠️ **第一版文案犯过这个错**：网关明明是被杀掉的，App 却说「连不上那台服务器」，把用户指向了错误的方向。⭐ 另外两档实测通过：没装 Tailscale 时报 DNS 解析失败；停掉 ComfyUI 时报「网关在线，但 ComfyUI 没起来」并带回网关给的原因。⚠️ `ConnectionRefused` 那一档**至今没有被真实触发过**，按 §3 规则 ③ 不算验证过。原计划说明：⚠️ **四种情况要真的分别制造出来，不许 mock 异常类型**。⭐ **判据分两层**：前三种看传输层异常，第四种看**响应体** —— `/v1/health` 即使 ComfyUI 挂了也返回 200（2026-09-24 读 `server/app/main.py` 核实，是故意的设计）。⚠️ **中间两种很可能分不开**：网关跑在 WSL 里而 tailnet 节点是 Windows 主机，WSL 停掉时到 7869 的连接是被拒绝还是超时**没有测过**；Hyper-V 防火墙曾把 WSL 端口从外面全挡住（ContextPack §4.7），那时表现可能是超时。**实测与架构文档 §5 那张表不符时改表，不改判据**（§4.1）。真分不开就如实写成「服务器或网关不可达」并说清为什么 |
| VS-32 | 真机填入 MagicDNS 地址后连接页显示网关在线，并显示 `/v1/health` 的版本信息 | 3 | M2R1 | ✅ **M2R1**。真机（PJA110 / Android 13）显示网关版本 `c9b0873`、GPU 名与显存读数。⭐ **显存读数是「它确实跑在那台服务器上」的证据**，和 M1R1 用 `vram_total` 证明部署成功是同一个手法 |
| VS-41 | **颜色字面量只出现在 `ui/theme/`**：扫 `app/src/main/java` 下每个 `.kt`，`Color(0x…)` 与 `#RRGGBB` 形式的颜色字面量不出现在 `ui/theme/` 以外 | 1 | M2R1 | ✅ **M2R1**。植入 `Color(0xFFFF0000)` 验证过会红，报出 `ConnectScreen.kt:322`。⚠️ 与 VS-30 **写在同一个测试文件里** —— 两者是同一个形式（扫源码、报具体文件与行号），分两处会有两份遍历代码。⚠️ 要故意在 `ui/connect/` 里植入一个 `Color(0xFFFF0000)` 确认它会红。它守的是**「换配色时改一处就够」**：颜色散进各页面后，换配色变成一次全仓库搜索替换，而那种替换会顺手改到不该改的地方**且不报错**。⭐ **「好不好看」本身是 Level 4，机器判不了** —— 这条守的是可改性，不是美观 |
| VS-33 | **上传**：真机从相册选图上传成功，返回的 `width`/`height` 与该图实际尺寸一致，`asset_id` 带 `grokgen/` 前缀 | 3 | M2R2 | ⬜ 计划。⚠️ 「尺寸一致」不是多余的——**不指定尺寸时画布就是按这两个数推的**，错了会让首帧被拉伸变形**且不报错**。⚠️ 必须用返回值里的 `asset_id`，不能拿本地文件名去拼（重名时 ComfyUI 会改名，网关侧 VS-15 测过） |
| VS-34 | **提交请求体与契约逐字段一致**：固定表单输入序列化出的 JSON 与 golden 文件相同 | 2 | M2R3 | ⬜ 计划。⚠️ 理由是网关的 pydantic **会把没声明的字段直接丢掉且不报错**（ContextPack §4.005 实测过）——App 多传一个字段或把 `null` 写成别的东西，请求照样 200、任务照样成功，只是行为不对。⚠️ **golden 不提供自动覆盖开关**，否则「改坏了」和「改好了」是同一个动作 |
| VS-35 | **`normalized` 与 `notices` 显示在界面上**：至少 `actual_duration_seconds`、`width`/`height`、`seed` 三项 | 3/4 | M2R3 | ⬜ 计划。理由见契约 §2：用户填 5 秒实际得到 5.17 秒、画布可能是按首帧图比例推算的、seed 是网关生成的。**不显示则用户以为模型不准，或永远复现不出某个结果** |
| VS-36 | **轮询在该停的时候停**：任务进终态后停止，App 退到后台时停止 | 2/3 | M2R4 | ⬜ 计划。⚠️ **判据是数请求次数**（给 API 套一层计数装饰器），不是「跑一会儿看日志」——后者判不出「多轮询了几次」 |
| VS-37 | **取消的三种情形**：排队中取消 → `cancelled`；运行中取消 → `cancelled`；**对终态任务取消返回 409 → 显示「已经结束了」，不是崩溃也不是「失败」** | 2/3 | M2R4 | ⬜ 计划。⚠️ 契约里 409 与 404 意思完全不同：404 是「不存在，别再问了」，409 是「状态不对/再等等」。**混成一个 App 就不知道要不要继续轮询**。`GET /v1/jobs/{id}/video` 在任务未 `done` 时也返回 409，同理 |
| VS-38 | **网络中断后任务仍在跑**：轮询途中关掉手机 Tailscale 几十秒再打开，任务最终仍走到 `done`，App 显示「连接中断，重试中」而不是「任务失败」 | 3 | M2R4 | ⬜ 计划。对应需求 §7 的「任务持久性」。⚠️ **把一次网络抖动显示成任务失败，用户会重新提交一次——那要再付一次 GPU** |
| VS-39 | **真机上能拖进度条**：拖到视频中段，**从拖到的位置开始播**，画面与音频都正确 | 3/4 | M2R5 | ⬜ 计划。⚠️ 判据不是「返回了 206」——网关那一层 M1R6/R7 已用 curl 验过并逐字节比对过。这里验的是**播放器怎么用这个能力**：响应头正确而播放器拿到错误字节，表现是拖动后画面错位，只有真拖一次才看得出来。另需记下两个没有合格线但必须有的数：**起播等多久**、**拖动后缓冲多久** |
| VS-40 | **一次完整的 I2VA 全程不碰电脑**：填地址 → 选图上传 → 填 prompt → 提交 → 看状态 → 播放，全部在手机上做完 | 3/4 | M2R5 | ⬜ 计划。⭐ **这条同时是 M1 退出标准的最后一块**（「能在手机上播放的 MP4」）。通过之后要在**同一次改动里**把 `Docs/TODO.md` 的 M1 改成 ✅ 并更新 `server/Docs/implementation/M1_gateway_mainline.md` §1.1 那张表——漏一处就会出现「一份说完成一份说没完成」 |

---

**VS-19 〜 VS-29 预留给网关（`server/`）。M2 起（Android 客户端）从 VS-30 开始编。**

⚠️ 这里改成了**分段预留**，取代原来「往后推」的写法。
原因：两条并行的编号流共用一个移动的前沿，必然反复相撞 ——
已经撞过两次（17→20，20→…），每次都要回来改这一句。
分段之后两端各自追加，互不影响。

**「编号只追加、不复用」这条规矩不变**，分段只是把两条流的起点分开。

### 三条关于覆盖面的记账规则

**① 「语料里没有」和「构造不出来」要分开记。**
某个分支在真实使用中从不触发，但输入构造得出来 ⇒ **补构造用例**。
某个分支任何输入都构造不出来 ⇒ **不要补端到端用例**，那只会得到一个绿色但与产品行为无关的测试；
在代码注释里写明它何时会变成可达。

**② 只证明"坏的消失了"不够，要有一个正向的数。**
例如验证对齐指令行时，"没有报错"和"输出里没有多余空标签"都是负向的；
**还要断言那一行确实出现了，且秒数是 `5.17` 而不是 `5.00`。**
把整段删掉也能让负向断言通过。

**③ 一条从未被触发过的回落分支，不算验证过。**

---

## 4. 两条不许违反的规矩

### 4.1 测完了不许改判据

实测没达到判据时，只有两种正当处置：

- **前提被推翻** ⇒ 回去改上游文档，并说清是哪条前提错了；
- **实现有难度** ⇒ 改代码。

**"把阈值放宽到刚好通过"两种都不是。** 判据是在测之前定的，
测完再改它，这条判据就永远不会失败 —— 而**一条从未失败过的判据不是判据**。

未通过就如实记成未通过，**跟着那个里程碑的退出评审走，不得在同步文档时消失**。

### 4.2 性能数字必须带测量条件

任何耗时、显存、吞吐的读数，记录时要一并写下：
**在哪台机器、什么负载状态、用的什么输入、跑了几次取的什么值**。

不带条件的性能数字**不可复现**，而"结论必须可复现"是 `AGENTS.md` §5 的第一条。
两次读数差很多时，没有这些条件就无法排查差异从哪来。

⚠️ 这一条在这个项目里格外容易出问题：**GPU 任务的耗时受显存是否已加载模型影响极大**
（模型加载是分钟级的固定开销，runbook §6.6）。
一个"生成用了 2 分钟"的读数，**不写清模型是否已经在显存里，等于没写**。

---

## 5. 每轮必交的验证报告

每个 round 结束时，在 `Docs/experience/YYYY-MM-DD/M<n>R<m>_*.md` 里记录：

- **验证了什么** —— 层级 + VS 编号 + 实际命令 + 实际输出
- **没验证什么** —— 以及为什么
- **哪些验证失败了**
- **下一轮遗留哪些风险**

⚠️ **「没验证什么」不是可选项。** 只写通过的部分，
会让下一轮的人误以为覆盖是完整的 —— 那比不写更糟。
