# TODO

**本文件是任务与里程碑的权威跟踪。** 项目总览看根 `README.md`，
当前会话上下文看 `Docs/ContextPack.md`，三者职责见 `AGENTS.md` §1.3。

⚠️ **只有这一份，不在 `server/Docs/` 或 `app/Docs/` 下另建。**
里程碑是跨两端的（M2 起两端都要动），状态分两处记就得读两份才知道项目在哪。

状态记号：⬜ 未开始 · 🔄 进行中 · ✅ 已完成 · ⏸ 阻塞 · ❌ 作废

---

## 里程碑

里程碑的内容与验收标准见 `Docs/requirement/grokgen_requirements_v0.1.md` §9。

| | 里程碑 | 涉及 | 状态 |
|---|---|---|---|
| M1 | 后端主链路：提交 H3 任务、报告状态、取回视频 | server | 🔄 网关侧完成，**退出标准差「手机上能播」一条**，等 M2 |
| M2 | Android 原型：连接、上传、提交、播放 | app + server | 🔄 **M2R1 完成**（连接），R2〜R5 未开始 |
| M3 | 媒体库：索引、缩略图、网格、在线播放 | server + app | ⬜ |
| M4 | 接入 SD：生图 + 服务切换 + 显存释放 | server + app | ⬜ |
| M5 | 跨服务组合：SD 生图 → 当作视频首帧 | 两端 | ⬜ |
| M6 | LoRA 自由挑选（视频与生图两边） | 两端 | ⬜ |
| M7 | 其余高级功能：REF2VA、多参考、收藏、批量下载 | 两端 | ⬜ |

⚠️ **2026-09-23 里程碑从 5 个拆成 7 个**，M1–M4 编号未变。
起因是需求扩大了两项（LoRA 自由挑选、参考图三个来源），见需求文档 §9。
**M5 和 M6 的顺序不要对调** —— M5 只是把两条已有链路接起来，
M6 要动两条链路各自的内部，而且有一个未解决的前置问题（哪个 LoRA 属于哪个底模）。

**v0.2 还有一个想法在记录中**：Civitai 浏览、下载与「照着做一遍」，
见 `Docs/requirement/grokgen_requirements_v0.2.md`。
⚠️ 那份文档里有**三个已实测确认的阻塞问题**，在它们有答案之前不要开工。

---

## M1 轮次

执行文档：`server/Docs/implementation/M1_gateway_mainline.md`
每轮的判据与 VS 编号见该文档 §3。

| 轮次 | 要解决的问题 | VS | 状态 |
|---|---|---|---|
| M1R1 | 网关骨架 + 部署链路首次实跑 | VS-12, VS-13 | ✅ |
| M1R2 | workflow 构造器正确 | VS-1〜4 | ✅ |
| M1R3 | ComfyUI 客户端 + 第一次真实生成 | VS-5〜7, VS-14 | ✅ |
| M1R4 | 上传 + 首帧真的生效 | VS-8, VS-15, VS-16 | ✅ |
| M1R5 | 任务状态机与串行队列 + 对外任务 API | VS-9, VS-10, VS-17, VS-18 | ✅ |
| M1R6 | 取回产物 + Range | VS-11 | ✅ |
| M1R7 | 端到端冒烟脚本 | VS-19, VS-20, 全部复跑 | ✅ |

---

## M2 轮次

执行文档：`app/Docs/implementation/M2_android_prototype.md`
架构：`app/Docs/architecture/android_client_architecture_v0.1.md`
每轮的判据见执行文档 §3，VS 编号的定义见 `Docs/Validation.md` §3。

| 轮次 | 要解决的问题 | VS | 状态 |
|---|---|---|---|
| M2R1 | 手机上的 App 能不能连上网关，连不上时说不说得清为什么（**外加视觉基础**） | VS-30, VS-31, VS-32, VS-41 | ✅ **VS-31 部分达成**，见下 |
| M2R2 | 相册里的图能不能变成一个 `asset_id` | VS-33 | ⬜ |
| M2R3 | 提交的请求体跟契约对不对得上 | VS-34, VS-35 | ⬜ |
| M2R4 | 状态能不能一路看到终态，能不能取消 | VS-36, VS-37, VS-38 | ⬜ |
| M2R5 | **手机上到底播不播得动**（同时收掉 M1 的退出标准） | VS-39, VS-40 | ⬜ |

⚠️ **M2R1 的 VS-31 只部分达成，如实记着**：四种连接状态都在真机上真的制造出来了，
但**「服务器离线」与「网关进程没起来」分不开** —— 这台服务器的 WSL 用镜像网络，
关闭端口上的连接是被丢弃而不是被拒绝，两种都只表现为超时。
已按 `Docs/Validation.md` §4.1 改上游（需求 F1、App 架构 §5.0），合并成一档。
⚠️ **换一台不用镜像网络的机器，这两种就又分得开了** —— 这不是代码的问题。

⭐ **M2R1 顺带修了一个网关的 bug**：`/v1/health` 在 ComfyUI 挂掉时**永远不返回** ——
httpx 里请求级的 `timeout=None` 意思是「不设超时」而不是「用默认值」。
`queue()` 也中招，那条更严重（它在任务轮询循环里）。已修并补了回归测试。

⭐ **视觉不设独立里程碑**（2026-09-23 决定，需求 §7.1.5）。拆成两块：
**视觉基础**（配色、字号层级、间距、圆角、状态色）放 **M2R1**，写一次全 App 继承；
**逐页打磨**跟着各自的页面走，**每一轮做出来的页面在那一轮里就要打磨到能看**。
⚠️ 「最后设一个 UI 美化里程碑」是最差的选项：UI 代码写两遍，
而且要在一次改动里同时动七八个页面 —— 而 UI 只能靠人眼判，那时没法定位是哪一处改坏的。

⚠️ **App 的自动化测试能覆盖的东西比网关少得多。** 网关那边 workflow 构造、
帧数换算、状态机都是纯逻辑，开发机上就能测；
**App 这边真正的风险（播放、上传、网络分支）全都必须上真机** ——
所以每一轮的判据都落在真机上，`./gradlew test` 只是入场券。

---

## 已完成的准备工作

| 事项 | 完成于 | 记录在 |
|---|---|---|
| ✅ 需求整理（从 chat_dialogue.json） | 2026-09-22 | `Docs/requirement/` |
| ✅ 网关架构定稿 | 2026-09-22 | `server/Docs/architecture/` |
| ✅ 接口契约初稿 | 2026-09-22 | `Docs/contract/` |
| ✅ 验证标准 | 2026-09-22 | `Docs/Validation.md` |
| ✅ M1 执行文档 | 2026-09-22 | `server/Docs/implementation/` |
| ✅ git 仓库 + 服务器 clone + 部署脚本 | 2026-09-22 | `scripts/deploy_server.sh` |
| ✅ SD 模型软链接归位并验证 | 2026-09-22 | M1 执行文档 §5 |
| ✅ M2 架构 + 执行文档 | 2026-09-23 | `app/Docs/` |

---

## 未决事项

按「什么时候必须解决」排，不按提出时间。

### 挡住 M1

*（当前没有）*

### 已解决（M1R7 补跑）

✅ ~~冷启动耗时~~ —— 实测：显存清空后重载 **94 秒**（热启动 75 秒，只多付约 19 秒）。
`job_timeout_seconds` 据此定为 **1800 秒**。
✅ **「ComfyUI 进程重启后」那一次 M2R1 也补测了：92 秒**，见下面「悬着的」那一节。

✅ ~~7869 的防火墙规则~~ —— 已加，三条规则全部限定来源为 Tailscale 的
IPv4 与 IPv6 地址段，脚本改成了能自愈（以前重跑不会收紧旧规则）。

✅ ~~`running` 状态在真实链路上是否可达~~ —— M1R7 冒烟实测通过，
转移轨迹 `preparing@0s → running@1s → done@75s`。

✅ ~~`interrupt()` 之后 `/history` 里那条记录长什么样~~ —— M1R7 实测，
**而且推翻了代码里的假设**：`status_str="error"` 但 **`completed=False`**。
网关原来用 `completed` 判断「执行结束了没有」，那个字段的真实含义是
「成功完成」—— 于是被中断和报错的任务永远等不到终态。已修（见 M1R6R7 总结 §1）。

### 挡住 M2（Android 原型）

- ✅ ~~**M2 还没有执行文档**~~ —— 2026-09-23 已补齐：
  `app/Docs/architecture/android_client_architecture_v0.1.md` 与
  `app/Docs/implementation/M2_android_prototype.md`。轮次划分见下面的「M2 轮次」。
- ✅ ~~**进度怎么推给 App**~~ —— **定了：App 轮询，网关不向 App 推送。**
  判断依据是采样只有 8 步（约每 9 秒一个进度更新），
  而 2 秒一次的轮询在 75 秒里能问到约 37 次 —— **推送没有多出来的信息可以给**；
  且只有一个用户，轮询的服务端压力为零。
  决定与完整理由记在契约 §6.1 与 App 架构文档 §1。
  ⚠️ **将来真要推送时先看 SSE，不要直接上 WebSocket。**
- ⬜ **⚠️ 上面那条只决定了「网关 → App」这一段，「网关 → ComfyUI」那一段仍要做。**
  `stage` 与 `progress` 的数据**只能**来自 ComfyUI 的 WebSocket 事件 ——
  网关现有的 `/queue`（问队列里有哪些任务）与 `/history/{id}`（问最终结果）
  **都回答不了「现在跑到第几步」**。不连 ComfyUI 的 ws，这两个字段就永远是空的，
  而需求 F2 点名要求单独显示分钟级的 `loading_model`。
  ⚠️ **「ComfyUI 没有别的 HTTP 途径能拿到当前执行进度」这一点还没核实过** ——
  它是从网关已实现的三个接口推出来的，没去 ComfyUI 源码里确认。**动手前先确认一次。**
- ⬜ **M2 要顺带把 M1 的退出标准收尾**：「下载回一个**能在手机上播放的** MP4」
  这一条只有真机播过才算，所以 M1 现在是 🔄 不是 ✅。
  收尾的动作定在 **M2R5 的 VS-40**，通过后要在**同一次改动里**改两个地方：
  本文件的 M1 状态，以及 `server/Docs/implementation/M1_gateway_mainline.md` §1.1 那张表。
- 🔄 **接口契约按实际实现回填**：任务那一节（§2）M1R5 已回填 ——
  删掉了 `advanced`（它把两套采样 profile 的旋钮单独暴露出去，
  等于让需求文档 §8.2 犯过的混用错误重新变成可达状态）、换成 `turbo`；
  `normalized` 补齐九个字段；定义了列表接口的响应形状与取消的语义。
  **剩下 §4 媒体库、§5 GPU、§6 事件流仍是草案**，各自等 M3 / M4 / M2。

### 挡住 M3（媒体库）

- ⬜ **⚠️ `job_id` 的序号在网关重启后会重复**：序号是进程内的，
  重启后从 `0001` 重来，同一天重启两次就会产生两个相同的 `job_id`。
  M1 状态不持久化、重启后旧 id 本来也查不到，所以现在影响为零 ——
  但**做持久化时必须先解决它**，否则会撞主键。
- ⬜ **任务与媒体的元数据存哪**：SQLite 还是 JSON 索引文件。
  倾向 SQLite（要分页和按来源过滤）。
- ⬜ **缩略图什么时候生成**：任务结束时顺手做，还是首次访问时懒生成。
  倾向前者，那时文件刚写完、磁盘缓存还热。
- ⬜ **任务状态持久化**：M1 只存内存，网关一重启就丢。

### 挡住 M4（接入 SD）

- ⬜ **SD 的两代模型怎么处理**：`v1-5-pruned-emaonly` 是 SD 1.5，
  其余三个（6.5–6.7 GB）看着像 SDXL 系。**两代 workflow 不一样** ——
  要么只支持一代，要么按模型类型分别拼图。

### 挡住 M5（SD 生图 → 当视频首帧）

- ⬜ **做成一个复合任务还是两个独立任务**：倾向两个独立、UI 上串起来
  （理由见需求 F8.1 ②：用户多半想先看看那张图行不行，而生成视频贵得多），
  **但没定**。它影响任务状态机的形状 —— 要在动手之前定，不是边做边定。
- ⬜ **媒体库的图怎么给 `LoadImage` 用**：产物在 `output/`，而那个节点读 `input/`。
  软链接、复制、还是让 workflow 读绝对路径（架构 §3.3）。
  ⚠️ **无论选哪条都必须幂等** —— 同一张图反复拿去生成很正常，
  每次复制一份会让那个已经没有清理机制的目录迅速膨胀。
  ⚠️ 这条**同时挡着 M3**（来源 2 也要用它）。

### 挡住 M6（LoRA 自由挑选）

- ⬜ **怎么判断一个 LoRA 属于哪个底模** —— 这是 M6 唯一的真难点。
  ComfyUI 只给平铺的文件名列表，不带底模信息，而服务器上 H3 的 4 个
  和 SD 的 35 个混在同一个目录下。文件名不可靠（`MysticXXX_MMH3-V4`
  看得出，`Elieen` 看不出）。三条候选见需求 §8.6，各有代价。
- ⬜ **那份底模账本要能同时容纳两种来源**：手工登记的，和将来下载时自动记的。
  不要设计成只有后者（v0.2 的 Civitai 功能会自动带 `baseModel`，
  但解决不了存量那 39 个）。

### 悬着的，但不挡当前轮次

- ⬜ **对齐指令行两种写法哪种对**：Director 文档带 `<>` `[]`，V23 模板示例不带。
  M1R4 首次真正用到（I2VA 带对齐行），**结果正常** —— 但那只证明当前写法能用，
  证明不了另一种**不会更好**。仍然没有便宜的验证方式。
- ✅ ~~turbo LoRA 与 `h3ErosMax_beta5_fp8` 是否兼容~~ —— M1R3 实跑通过，没有 shape mismatch。
- ⬜ **那条 Hyper-V 防火墙规则为什么会变**：M1R3 中途 WSL 的端口从外面全连不上，
  根因是入站被 Block 且只放行环回。已加规则恢复，但**是什么改成那样的没查出来，
  可能再次发生**（runbook §6.5）。
- ⬜ **`input/grokgen/` 的上传图没有清理机制**，会一直堆积。M3 媒体库一起处理。
- ⬜ **FL2VA（首尾帧）一次都没真跑过**，尾帧那条路径至今只有单元测试。
- ⬜ **⚠️ 失败路径的真实形状只见过一种。** M1R7 见到了「被中断」长什么样，
  但**显存不足、ComfyUI 进程中途挂掉**仍然没见过。
  `completed` 那个坑说明：这一类假设只要没见过真实样本，就可能整条都是错的。
- ✅ ~~**ComfyUI 进程重启后第一次生成要多久**~~ —— **M2R1 实测 92 秒**，
  而且**推翻了「分钟级」那个说法**：它比 `/free` 之后那次（94 秒）还略快。
  测量条件比 M1R7 更彻底（WSL 刚 `wsl --shutdown` 过，页面缓存也是空的）。
  ⚠️ **但另有一笔**：ComfyUI 从启动到端口能应答要 **66 秒**，那 92 秒不含它。
  ⇒ 对 M4：`/free` 与重启进程的生成耗时基本相同，**真正的差别是重启要多付 66 秒的端口就绪时间**。
- ⬜ **家庭局域网到底进不进得来只有推断，没有实测**。确认办法：
  手机连家里 Wi-Fi、关掉 Tailscale，访问 `http://192.168.71.11:7869/v1/health`。
- ⬜ **冒烟产物会堆积在 `output/video/`**，每跑一次多 1–2 个 mp4。
  **故意不自动清理** —— 它们是人眼判定的唯一材料，脚本删掉等于销毁证据。

### 不挡任何里程碑

- ⬜ **`server/requirements.lock.txt` 从未被用来安装过**。它是 M1R1 部署后从服务器
  `pip freeze` 出来的记录，部署脚本装的仍是不锁版本的 `requirements.txt`。
  真要靠它复现环境时可能装不出来。
- ⬜ **`HttpComfyClient` 的错误分支一条都没实际触发过**（超时、非 2xx、`devices` 为空）。
  按分层它豁免于开发机测试，但「只有代码没有证据」这件事要记着。
  M1R5 新增的 `delete_queued()` 也在这一类里：它只在
  「ComfyUI 队列里有别人的任务排在前面」时才可达，而那要手工在浏览器里提交才会出现。
- ⬜ **网关升级成 systemd 服务**（自动重启、开机自起）。
  需要一次交互式 sudo 或 `loginctl enable-linger`。
  现在用 `setsid nohup`，和 ComfyUI 一样。
- ⬜ **把 ComfyUI 收回到只监听 `127.0.0.1`**。
  等 M2 的 App 打通后再做，现在两个入口并存不影响什么。
